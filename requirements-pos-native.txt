# Keep the Python bindings and bundled Qt DLLs on this verified compatible
# pair. An unpinned pair can work from source yet fail after packaging.
PyQt6==6.9.1
PyQt6-Qt6==6.9.2
requests>=2.28.0
openpyxl>=3.1.0
psycopg[binary]>=3.1.0
# Optional: building a separate Windows executable
pyinstaller>=6.0.0
