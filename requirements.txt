# KAY POS desktop runtime
# Versions installed on Windows / Python 3.14.7 on this workstation.
# Install: python -m pip install -r requirements.txt
# Direct dependencies are pinned; pip resolves transitive dependencies.

# Desktop UI (YouTube/WebEngine removed).
PyQt6==6.11.0

# Logging, HTTP integrations, and update version checks.
loguru==0.7.3
requests==2.34.2
packaging==26.3

# Charts, images, Excel, barcodes, and QR codes.
numpy==2.5.2
matplotlib==3.11.1
Pillow==12.3.0
openpyxl==3.1.5
python-barcode==0.16.1
qrcode[pil]==8.2

# Local PostgreSQL and attendance devices.
psycopg[binary]==3.3.4
pyzk==0.9

# Local HTTP services / Touch endpoints.
fastapi==0.141.1
pydantic==2.13.4
python-multipart==0.0.32
uvicorn[standard]==0.52.1

# Optional voice input is not part of the current installation.
# It needs SpeechRecognition, PyAudio and sounddevice; typing works without it.
# PyInstaller is a build tool and is not needed to run main.py.
